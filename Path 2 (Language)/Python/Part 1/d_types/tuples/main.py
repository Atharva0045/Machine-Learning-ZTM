# Tuples are immtable lists..
# we can do any list methods that require no modifications.. i.e. access, find, checking, etc.
# one can also do slicing
# tuples can also act as key in dictionaries as they are immutable

example = (10, 20, 30, 40)

print(example[0:3])